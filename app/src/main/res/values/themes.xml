<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SpeakSwift" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">#ffffff</item>
        <item name="colorPrimaryDark">#bfbdbd</item>
    </style>

    <style name="Theme.SpeakSwift" parent="Base.Theme.SpeakSwift" />

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="BottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <!-- Cambia el color de fondo del BottomNavigationView -->
        <item name="android:background">#0065ad</item>

        <!-- Cambia el color de los iconos inactivos -->
        <item name="itemIconTint">#ffffff</item>

        <!-- Cambia el color del texto de los elementos inactivos -->
        <item name="itemTextColor">#ffffff</item>

    </style>


</resources>