<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="EstiloRadioButtonSeleccionado" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <!-- Personaliza los atributos para cuando está seleccionado -->
        <item name="android:textColor">@color/white</item>
        <item name="android:backgroundTint">#0065ad</item>
        <item name="android:textSize">14dp</item>
        <item name="android:fontFamily">@font/montserrat_bold</item>
        <item name="cornerRadius">20dp</item>
        <!-- Otros atributos... -->
    </style>

    <style name="EstiloRadioButtonNoSeleccionado" parent="Widget.AppCompat.CompoundButton.RadioButton">
        <!-- Personaliza los atributos para cuando está seleccionado -->
        <item name="android:textColor">#000000</item>
        <item name="android:backgroundTint">@color/white</item>
        <item name="android:textSize">14dp</item>
        <item name="android:fontFamily">@font/montserrat_semibold</item>
        <!-- Otros atributos... -->
    </style>

</resources>